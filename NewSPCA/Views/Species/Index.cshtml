@model IEnumerable<NewSPCA.Models.Species>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Species_Name)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Species_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SpeciesID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SpeciesID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SpeciesID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
